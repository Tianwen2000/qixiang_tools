# 部署与更新指南

> 记录生产服务器的部署架构，以及改完代码后如何更新上线。配合 [项目常用命令汇总](./项目常用命令汇总.md) 使用。
> 出于安全，本文里服务器 IP、密码一律用占位符 `<服务器IP>` 表示，不写真实值。

## 1. 部署架构

| 项 | 说明 |
|---|---|
| 服务器 | Ubuntu，项目目录 `/home/ubuntu/qixiang_tools` |
| 前端 | nginx 静态托管 `frontend/dist`（站点 `/etc/nginx/sites-enabled/qixiang_tools`，`root .../frontend/dist`） |
| 接口反代 | nginx 把 `/api` 反代到 `http://127.0.0.1:8000/api/` |
| 后端 | systemd 服务 `qixiang-backend`（uvicorn，端口 8000），虚拟环境 `backend/.server-venv` |
| 当前同步方式 | **手动打包上传（方式一）——服务器上没有 `.git`，`git pull` 用不了** |

> `frontend/dist` 是构建产物，但本项目当前为配合现有部署方式有意纳入 Git 跟踪。本地改前端必须先 `npm run build`，再提交/上传源码与对应的 `dist`；若将来改为服务器或 CI 构建，再统一调整这一策略。

下面给两种更新方式：**方式一**是现在服务器实际用的；**方式二**（git）更省事、且没有权限坑，推荐以后切过去。

---

## 2. 方式一：手动打包上传（服务器当前用的方式）

服务器是从本地用 scp / 工具上传的，没有 git 仓库，所以只能传文件。

### 2.1 本地打包
```bash
cd /Users/a123/PycharmProjects/qixiang_tools/frontend && npm run build   # 改了前端就重新打包

cd /Users/a123/PycharmProjects/qixiang_tools
# 按本次改动按需增减要打包的文件
tar czf /tmp/qx_update.tgz --exclude='._*' --exclude='__pycache__' \
  frontend/dist \
  backend/app/tools/<改动的模块>.py \
  backend/app/utils/validators.py \
  backend/app/configs/tools.json
```

### 2.2 上传 + 解压 + 重启
```bash
scp /tmp/qx_update.tgz ubuntu@<服务器IP>:/tmp/
ssh ubuntu@<服务器IP>
# —— 以下在服务器上执行 ——
cd /home/ubuntu/qixiang_tools && tar xzf /tmp/qx_update.tgz
sudo systemctl restart qixiang-backend     # 改了后端才需要
```

### 2.3 ⚠️ 关键：修正文件权限（否则 nginx 403）
**从 Mac 上传的文件常带 `600` 权限，nginx 读不了会报 403（背景 mp4 最容易中招）。** 解压后务必把目录设 755、文件设 644：
```bash
find /home/ubuntu/qixiang_tools/frontend/dist -type d -exec chmod 755 {} +
find /home/ubuntu/qixiang_tools/frontend/dist -type f -exec chmod 644 {} +
```

现在最推荐的更新流程（后续更新发布同步新代码到服务器）

若改前端本地前端需先构建：
cd /Users/a123/PycharmProjects/qixiang_tools/frontend
npm run build
tip：
前端：npm run build → 生成静态文件 → rsync 上传 → Nginx 直接读取这些文件（如果服务器里面前端不构建，仍需单独启动前端hhh）
后端：rsync 上传代码 → systemctl restart qixiang-backend → 后端服务运行
本地提交到GitHub（需提前确定远程仓库是否对应正确）：
cd /Users/a123/PycharmProjects/qixiang_tools
git add -A
git commit -m "更新说明"
git push
然后：在本地输入一条命令同步更新到服务器且排除差异文件（排除命令表示：服务器上 /home/ubuntu/qixiang_tools/ 里有，但是本地没有的文件会被删掉。所以如果服务器项目目录里有这些文件，一定要把它们加到排除列表）：（这一条命令输入之后需要输入服务器密码）
rsync -az --delete \
  --chmod=Du=rwx,Dgo=rx,Fu=rw,Fgo=r \
  --exclude .git/ \
  --exclude backend/.venv/ \
  --exclude backend/.server-venv/ \
  --exclude frontend/node_modules/ \
  --exclude __pycache__/ \
  --exclude '*.pyc' \
  --exclude .DS_Store \
  --exclude '._*' \
  --exclude .env \
  --exclude backend/.env \
  --exclude 'backend/.mysql-*' \
  --exclude uploads/ \
  --exclude logs/ \
  --exclude '*.sqlite3' \
  --exclude '*.db' \
  --exclude .idea/ \
  --exclude .vscode/ \
  --exclude .venv/ \
  --exclude .pytest_cache/ \
  --exclude backend/app/temp/ \
  --exclude frontend/test-results/ \
  --exclude log/ \
  --exclude '*.log' \
  --exclude .env.local \
  --exclude '*.sqlite' \
  -e "ssh -o StrictHostKeyChecking=accept-new" \
  /Users/a123/PycharmProjects/qixiang_tools/ \
  ubuntu@82.156.106.68:/home/ubuntu/qixiang_tools/
  
  # 注意：.env 等文件如果有改动，不要通过rsync的方式同步到服务器，最好还是在服务器上手动同步或者单独同步这一个
  
ubuntu@82.156.106.68's password: 
cannot delete non-empty directory: docs/feedback
# 意思就是rsync在跟本地dev同步的时候想删除服务器上的 docs/feedback 目录，但这个目录里面还有文件，所以没删掉。
# 如果确定没啥用的文件，就进去服务器把它删了
sudo rm -rf /home/ubuntu/qixiang_tools/docs/feedback

然后：如果更新迭代的时候改了 Python 依赖，比如 requirements.txt，光 rsync + restart 不行。需要在服务器里面执行：（不然调用工具会报错调用失败）记得先进服务器
记得先进服务器：ssh ubuntu@82.156.106.68

一行：
cd /home/ubuntu/qixiang_tools/backend && source .server-venv/bin/activate && pip install -r requirements.txt && sudo systemctl restart qixiang-backend

多行：
cd /home/ubuntu/qixiang_tools/backend
source .server-venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart qixiang-backend

# 要记得还要在服务器里面重启后端，后面命令有！
然后：如果改了浏览器依赖或者其他外部依赖，除了装 requirements，还要安装 Chromium和其他依赖，需要在服务器里面执行,这里不做赘述，其他笔记文档里有

最后：重启后端
sudo systemctl restart qixiang-backend
sudo systemctl status qixiang-backend --no-pager
查看日志：
sudo journalctl -u qixiang-backend -n 100 --no-pager
持续查看：
sudo journalctl -u qixiang-backend -f
常见问题
Chromium 不存在
如果报：
Executable doesn't exist at /home/ubuntu/qixiang_tools/.playwright-browsers/...
重新执行：
PLAYWRIGHT_BROWSERS_PATH=/home/ubuntu/qixiang_tools/.playwright-browsers \
PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright \
./backend/.server-venv/bin/python -m playwright install chromium
命令行可用，但接口里不可用，优先检查 systemd 环境变量：
systemctl show qixiang-backend -p Environment --no-pager
确认里面有：
PLAYWRIGHT_BROWSERS_PATH=/home/ubuntu/qixiang_tools/.playwright-browsers
没有的话重新编辑：
sudo EDITOR=vim systemctl edit qixiang-backend
sudo systemctl daemon-reload
sudo systemctl restart qixiang-backend

FastAPI 异步环境报错
如果报：
It looks like you are using Playwright Sync API inside the asyncio loop.
说明在 async 接口里直接用了 sync_playwright()。
处理方式：改用 Playwright Async API；或把 Sync API 放到独立线程里执行。

然后：如果你改了数据库结构，比如新增字段、表结构变化，需要执行迁移命令。具体命令要看你项目用的是 Alembic、Django migration，还是自己建表。（本项目只有一个登录ai机器人涉及到sql，且用的是docker）；而且我在后端启动建表逻辑里加了兼容补列：加入一个反馈表的feedback.ip、feedback.device_id、logs.device_id这列是新增列，我只需要同步代码到服务器之后，后端服务重新启动就会执行 ensure_schema() ，如果表已经存在但这些列不存在，会自动 ALTER TABLE ADD COLUMN 补上。
一般这几种情况才建议手动迁移数据库：
1改字段类型：比如 VARCHAR(64) 改成 TEXT、INT 改成 BIGINT，这类可能影响已有数据。
2删除字段或重命名字段：自动补列只能“加”，不能安全判断怎么删、怎么改名。
3字段要从可空改成不可空，并且没有默认值：旧数据可能不符合新约束，需要先清洗数据。
4新增索引、唯一约束、外键：尤其数据量大时，建索引可能锁表，最好手动安排时间窗口。
5新建表但有复杂初始化数据：如果只是空表，create_all 可以自动建；如果要导入默认配置、权限、菜单等，就适合写迁移脚本。
6数据量变大以后：不是因为“大”本身必须迁移，而是大表执行 ALTER TABLE、加索引、改字段会更慢、更容易锁表，所以要手动控制。


最后登录云服务器，重启后端服务，并检查服务状态（一条命令）：（这一条命令输入之后需要输入服务器密码）
单行命令：
ssh -o StrictHostKeyChecking=accept-new ubuntu@82.156.106.68 \
  "cd /home/ubuntu/qixiang_tools && sudo systemctl restart qixiang-backend && sleep 3 && systemctl is-active qixiang-backend && curl -sS http://127.0.0.1:8000/api/health && echo"
多行命令：
ssh ubuntu@82.156.106.68
sudo systemctl restart qixiang-backend
systemctl is-active qixiang-backend
curl http://127.0.0.1:8000/api/health
curl -I http://127.0.0.1/

一句话总结
现在你用的是：本地 Git 记录版本 + GitHub 备份 + rsync 同步服务器 + systemd 重启后端
不是：服务器 git pull 部署



---

## 3. 方式二：git 部署（推荐，将来切过去）

把服务器项目改成 git 仓库后，更新会简单很多、也不会有权限坑：

```bash
# 一次性切换（在服务器上，注意先备份 backend/.server-venv 等本地产物）
cd /home/ubuntu
git clone git@github.com:Tianwen2000/qixiang_tools.git qixiang_tools_git
# 迁移服务器专属的 .server-venv、frontend/.env.local 等，再把 nginx root / systemd 指到新目录

# 以后每次更新：
cd /home/ubuntu/qixiang_tools && git pull
sudo systemctl restart qixiang-backend     # 改了后端才需要
```

git 方式：前端 `git pull` 后 nginx 直接生效（浏览器硬刷新），保留正确权限、无需手动打包，**强烈建议尽快切到这种方式**。

---

## 4. 按改动类型要做什么

| 改了… | 操作 |
|---|---|
| 只改前端 | 重新 `npm run build` → 传 `dist`（或 `git pull`）→ 浏览器硬刷新 |
| 改了后端 `.py` | 传/pull 后 `sudo systemctl restart qixiang-backend` |
| 改了 `requirements.txt` | 先 `./backend/.server-venv/bin/pip install -r backend/requirements.txt` 再 restart |
| 改了 nginx 配置 | `sudo nginx -t` → `sudo systemctl reload nginx` |

## 5. 验证
```bash
systemctl is-active qixiang-backend                                          # active
curl -s 127.0.0.1:8000/api/health                                            # {"code":0,...}
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1/wallpaper/spring.mp4   # 200（不是 403）
```

## 6. 常见问题
- **背景 mp4 / 图片 403**：文件权限是 `600`，nginx 读不了 → 按 2.3 用 `chmod 644` 修。
- **前端没更新**：忘了 `npm run build` 或没传 `dist`；或浏览器缓存 → 硬刷新。
- **后端 500 / 起不来**：`sudo journalctl -u qixiang-backend -n 80 --no-pager` 看日志（常见是依赖没装或代码报错）。
- **`/api` 502**：后端没起来 → 先 `systemctl is-active qixiang-backend`。

## 7. 关键路径速查
- 项目目录：`/home/ubuntu/qixiang_tools`
- 前端产物（nginx root）：`frontend/dist`
- 后端入口：`backend/app/main.py`（systemd `qixiang-backend`）
- 后端虚拟环境：`backend/.server-venv`
- nginx 站点配置：`/etc/nginx/sites-enabled/qixiang_tools`
- 后端日志：`sudo journalctl -u qixiang-backend`
